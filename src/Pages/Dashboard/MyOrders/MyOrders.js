import React from 'react';
import useTitle from '../../../hooks/useTitle';

const MyOrders = () => {
    useTitle('My Orders');
    return (
        <div>
            This is my orders
        </div>
    );
};

export default MyOrders;